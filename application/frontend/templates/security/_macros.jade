| {% macro render_field_with_errors(field) %}
p
  label(for="{{ field.name }}") {{ _(field.label.text) }}
  | {{ field(**kwargs)|safe }}
  | {% if field.errors %}
  ul
    | {% for error in field.errors %}
    li {{ error }}
    | {% endfor %}
  | {% endif %}
| {% endmacro %}
| {% macro render_field(field) %}
p
  input(name='{{ field.name }}', type='{{ field.name }}', id='{{ field.id }}', value='{{ _(field.label.text) }}')
//  {{ field(**kwargs)|safe }}
| {% endmacro %}