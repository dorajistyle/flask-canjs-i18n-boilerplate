#!/usr/bin/python
import os
node_modules = './node_modules'
if not os.path.exists(node_modules):
    os.makedirs(node_modules)
    os.system('npm install jsdoc --save-dev')
os.chdir('./application/frontend/compiler')
if not os.path.exists(node_modules):
    os.makedirs(node_modules)
    os.system('npm install can-compile --save-dev')
    os.system('npm install grunt-shell --save-dev')
    os.system('npm install grunt-contrib-watch --save-dev')
    os.system('npm install time-grunt --save-dev')
